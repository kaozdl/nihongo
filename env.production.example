# Production Environment Configuration
# Copy this file to .env on your production server

# Flask Environment
FLASK_ENV=production
FLASK_APP=app.py
FLASK_DEBUG=0

# Secret Key (REQUIRED - generate a strong random string)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-CHANGE-THIS

# Database Configuration (REQUIRED)
# PostgreSQL connection string format:
# postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/jlpt_db

# Alternative for Heroku/cloud platforms (they often provide DATABASE_URL)
# DATABASE_URL will be automatically provided

# SQL Echo (set to False in production to reduce logs)
SQL_ECHO=False

# Server Configuration (optional - depends on your hosting)
FLASK_RUN_HOST=0.0.0.0
FLASK_RUN_PORT=5000

# Optional: Additional production settings
# PREFERRED_URL_SCHEME=https
# SESSION_COOKIE_DOMAIN=.yourdomain.com

